node_modules
.next
src/generated
out
build
*.tsbuildinfo
next-env.d.ts
.env
.env.local
.git
.github
tmp
npm-debug.log*
# Данные, живущие в Docker-томах/bind-mount в рантайме — не должны попадать
# в образ через COPY . . (иначе именованный volume при первом создании
# "засеивается" содержимым образа, включая случайные тестовые файлы с хоста;
# для backups это ещё и потенциальный слив дампов БД в артефакт сборки).
public/uploads
backups
e2e
playwright-report
test-results
blob-report
